basic logic for bs 



          arr[mid] == target?
                 |
          YES → return mid
                 |
                NO
                 ↓
      arr[mid] < target?
          /           \
        YES            NO
         ↓              ↓
   move low right   move high left
   low = mid + 1    high = mid - 1



   if the middle is smaller than the target, move low right
   if the middle is bigger than the target, move high left





                low → high → mid
                    ↓
                compare middle with target
                    ↓
                found? → return mid
                smaller? → low moves right
                bigger? → high moves left
                    ↓
                repeat
                    ↓
                nothing left → return -1





first occurrence → found → search left
last occurrence → found → search right